#!/bin/sh

#set -e

ITERATIONS=-
INTERVAL=0.5
COLUMN=%CPU

i=$ITERATIONS
while [ $ITERATIONS = '-' ] || [ $i -ne 0 ]; do
    top -n 1 -o $COLUMN -b|head -n8|tail -n1
    sleep $INTERVAL
    if [ $ITERATIONS != '-' ]; then
	i=$((i-1))
    fi
done
